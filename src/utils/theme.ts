import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // 你的主要颜色
    },
    secondary: {
      main: "#dc004e", // 你的次要颜色
    },
  },
  // 其他主题设置
});

export default theme;
